NEXT ACTION
1. Test with echo 
	-> we need expand ~
	-> we need to expand $? to errcode
	-> in case only $, we need to pass it
	-> case: 
		echo $"42$" -> the result in bash is: 42$ but minishell is 42
		echo "$ " -> must show $ but we have empty
		echo '$?' -> must show $? but we show 0$?
		echo "'$?'" -> must show '0' but we show '?'
		echo "cat lol.c '|' cat > lol.c" must show cat lol.c '|' cat > lol.c but we show empty
		echo '$USER' "$USER" "text  ' text" must show $USER vinguyen text  ' text but we show empty
		echo $USER$TESTNOTFOUND$HOME$ must show vinguyen/home/vinguyen$ but we show empty
2. Test with export
	-> case:
		export TEST+=100 -> must add but we have not a valid identifier.